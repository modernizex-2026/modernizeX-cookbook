# ModernizeX: エンドツーエンドの COBOL→Java モダナイゼーションのための、グラウンディングされたエアギャップ対応の静的解析＋LLM パイプライン

**著者:** ModernizeX — レガシーモダナイゼーショングループ、ModernizeX Organization
**バージョン:** 1.0 · **日付:** 2026-07-25
**ステータス:** ワーキングペーパー

---

## 要旨

COBOL で書かれたメインフレームシステムは、依然として世界の金融・保険・行政に関わる取引の相当な部分を処理している。しかしながら、それらを保守できる技術者の数は減少し続けており、手作業による書き換えのコストは容認しがたいものとなっている。近年の大規模言語モデル（Large Language Models, LLM）はコードを翻訳できるものの、レガシーなモノリスに素朴に適用すると、識別子を幻覚（ハルシネーション）し、コンテキストウィンドウを超えるプログラム間でグローバルな文脈を見失い、さらに規制産業の企業にとって決定的な問題として、独自のソースコードをパブリッククラウドへアップロードすることが求められてしまう。本稿では、*決定論的な静的解析*と*グラウンディングされた LLM 生成*を組み合わせることで、COBOL/CICS/JCL システムを Java へモダナイズするエンドツーエンドのプラットフォーム **ModernizeX** を提示する。ModernizeX はまずレガシーコードベースを抽象構文木（Abstract Syntax Tree, AST）と、プログラム・データ・制御フローの各グラフ群へと解析し、続いて決定論的なコア部分を、レガシーシステムのロジックとアーキテクチャを保持するルールベースの手法によって Java へ移行する。本プラットフォームは **7 段階のパイプライン**（アセスメント → リバースエンジニアリング → コード生成 → コードリファクタリング → UI/UX モダナイゼーション → データ移行 → 検証）を駆動し、設計ドキュメント（SAD/SDD）、双方向のトレーサビリティマトリクス、移行済みソースコード、そしてモダナイズされたソースコード（リファクタリングされたサービスとモダナイズされたユーザーインターフェース）を生成する。これらはいずれもレガシーシステムへのトレーサビリティを保持している。我々はこのパイプラインを、完全なコードベースである **SAKURA-SMS**（68プログラム、30,635 LOC。索引/順編成ファイル上での対話画面＋バッチ処理）に対してエンドツーエンドで測定した。実行済みの単体テストおよび結合テスト仕様に至るまでの移行を **38営業時間** で完了し、生成されたコードは無修正でコンパイルが通り、全 68 モジュールにわたって単体テストキャンペーンを完了させた。その一方で、ソースコードの外部流出は皆無であることを保証した。本稿では、グラウンディングのための IR の設計、企業導入に必要なセキュリティおよびライセンシングのアーキテクチャ、限界、そして本手法を他のレガシー言語へ拡張するための方向性について論じる。

**キーワード:** レガシーモダナイゼーション、COBOL、メインフレーム、プログラム解析、抽象構文木、大規模言語モデル、コード翻訳、リバースエンジニアリング、トレーサビリティ、工数計測、エアギャップ環境での展開。

---

## 1. はじめに

数十年分のビジネスロジックが、IBM のメインフレームおよびミッドレンジシステム上で CICS と JCL の下で稼働する COBOL の中に符号化されたまま残されている。これらのシステムは安定している一方で、ますます*保守困難*になりつつある。すなわち、元の作成者は退職し、ドキュメントはコードから乖離するか、あるいはそもそも存在せず、その言語やプラットフォームは主流の技術者教育から姿を消している。そのため企業は、いずれも欠陥を抱えた次の二つの極端な選択肢の間で戦略的なジレンマに直面する。

1. **システムインテグレーターによる手作業での書き換え** — 遅く高価であり、しかも希少な COBOL の専門知識に律速される。企業は既存システムを*維持する*だけのためにすでに割増料金を支払っている [7]。狭い範囲に限定され、ツールで実行されるコード変換サービスでさえ、テスト・レビュー・結合作業を行う*前*の段階で 1 行あたり US$2.75 と公開価格が付けられており [6]、主要システム 1 件あたりの期間は通常 12〜18 か月に及ぶ。さらに、象徴的な基幹プラットフォーム刷新には遥かに長い期間を要しており、オーストラリア・コモンウェルス銀行の書き換えは 5 年と約 US$7億5,000万を要した [5]。
2. **素朴な LLM 翻訳** — 着手は速いものの、規模が大きくなると信頼性を欠き、かつ安全でない。汎用 LLM はコンパイラではなくテキスト予測器である。相互に依存する数千行の COBOL を与えられると、コンテキストウィンドウの外にあるグローバル変数を追跡できなくなり、構文や識別子を捏造し、さらにパブリックな Web または API インターフェース経由で駆動される場合には、顧客の独自コードがそのセキュリティ境界の外へ出ることを要求してしまう。

本研究を動機づける中核的な観察は、**素朴な LLM 手法の失敗は、言語モデリングそのものの失敗ではなく、*グラウンディングとコンテキスト*の失敗である**という点にある。COBOL プログラムは密結合したモノリスであり、単一のコピーブックが数十のプログラムにわたって共有され、制御は `CALL`、`LINK`、`XCTL` を通じてプログラム間で受け渡される。技術者であれ、モデルであれ、あるプログラムの*波及範囲（blast radius）*、すなわちそれが読み書きするデータ、それを呼び出すプログラム群、そしてそれが呼び出すプログラム群を把握せずに、そのプログラムを正しく書き換えることはできない。この情報はソースから静的解析によって*決定論的に復元可能*であり、言語モデルをまったく必要としない。我々の主張は、まず古典的なプログラム解析でこの構造を復元し、生のテキストではなく**標準化された中間表現**をモデルに手渡すならば、システムをアーキテクチャ的に妥当な状態に保ちつつ、モデルの幻覚と当て推量を最初のステップから排除できる、というものである。

さらに我々は、レガシーモダナイゼーションが単一のコード翻訳タスクではなく、*ドキュメント化と検証*のタスクであると観察する。規制対象の顧客は、翻訳されたコードだけを根拠に「メインフレームの電源を切る」ことはしない。むしろ彼らは、(a) 現行（as-is）システムをビジネスの言葉で説明する設計ドキュメント、(b) あらゆるレガシーコンポーネントがテスト済みのモダンコンポーネントへ対応することを証明するトレーサビリティマトリクス、そして (c) 同一の入力に対して新システムが旧システムの出力を再現することの証跡、を要求する。したがって、有用なレガシーモダナイゼーションプラットフォームは、理解・翻訳・検証を単一のパイプラインとして扱わなければならない。我々の計測はこれを裏付けている。工数が集中するのは翻訳ではなく（翻訳は決定論的な各段階によってほぼ無償化される）、リファクタリングレポート・テスト結果・結合テスト承認に対する人手のレビューが時間の大半を占める*検証*である。

本稿は次の貢献を行う。

- **グラウンディングされた二段階エンジン** — フェーズ 1 では決定論的な静的解析が、プログラム/データ/制御/画面/DB の各グラフを備えた標準化 AST を生成し、ルールベースの手法でコードをターゲットシステム（Java）へ移行する。続くフェーズ 2 では、グラウンディングされた LLM 生成がこの決定論的な基盤の上でモダナイゼーションを実施し、幻覚を低減しアーキテクチャの整合性を強制する。
- **エンドツーエンドの 7 段階パイプライン** — アセスメント、リバースエンジニアリング、コード生成、コードリファクタリング、UI/UX モダナイゼーション、データ移行、検証。これはアセスメント、グラフ解析、設計ドキュメント（SAD/SDD）のリバースエンジニアリング、COBOL→Java の移行とモダナイゼーション、Vue 3 フロントエンドへの画面モダナイゼーション、リレーショナルデータ移行、そして自動化された振る舞い等価性の単体テストおよび結合テストを統合するものであり、これらは人間参加型（human-in-the-loop）レビューを伴う双方向トレーサビリティマトリクスによって結び付けられる。
- **セキュリティおよび展開のアーキテクチャ** — 独自鍵持ち込み（Bring-Your-Own-Key, BYOK）方式の LLM ルーティングと、暗号学的に従量計測されるライセンシング。独自のソースコードが顧客のネットワークの外へ一切出ないように設計されており、銀行・保険・行政の購買層が抱く最大の反論に応えるものである。
- **完全なコードベースに対する工数の実測評価** — *SAKURA-SMS*（68プログラム/ 30,635 LOC、対話画面＋ISAM ファイル上のバッチ処理）を、実測されたオペレーター＋パイプライン時間の **38時間** でエンドツーエンド移行し、コンパイル可能なマルチモジュールの Spring Boot / Spring Batch プロジェクトと、全 68 モジュールにわたって完了した単体テストキャンペーンを生成した。

本稿の残りは次のように構成される。第 2 節では関連研究を概観する。第 3 節では本問題に対する成功基準を定義し、ModernizeX のアーキテクチャと 7 段階の方法論を詳細に述べる。第 4 節では評価の設定を、第 5 節では結果を提示し、第 6 節では限界と妥当性への脅威を論じる。第 7 節で結論を述べる。

---

## 2. 関連研究

### 2.1 レガシーコードの自動翻訳

ルールベースのトランスパイラや商用リファクタリングスイートは、以前から「ライク・フォー・ライク（like-for-like）」の COBOL から Java への変換を提供してきた。これらは決定論的で高速であるが、モダンなオブジェクト指向サービスではなく、COBOL のフラットで手続き的な構造をそのまま写した非イディオマティックな「JOBOL」（たとえば `PERFORM` ループや 88 レベルの条件を逐語的にレンダリングした Java）を生み出しがちである。また、方言固有の構文や埋め込み CICS/SQL の扱いにも苦戦する。ModernizeX は決定論的なコア部分についてはルールベースのジェネレーターを保持しており、我々の実測実行では、**無修正でコンパイルが通り、手作業のパッチ当てを要しない**完全なマルチモジュール Spring Boot / Spring Batch プロジェクトを生成する。しかし*イディオマティックな OOP、命名、例外処理へのリファクタリング*は別個の LLM 段階へ委ねることで、ルールがもつ速度と決定論性を、生成モデルがもつ柔軟性と組み合わせている。

### 2.2 コードのための大規模言語モデル

コード向け LLM は、主流言語に対するコード生成・要約・プログラム修復において高い性能を示してきた。しかしながら、汎用モデルやコード特化モデルでさえ COBOL やメインフレーム関連の成果物については学習が不足しており、この領域の公開ベンチマークも依然として限られている。XMainframe [1] のようなドメイン適応済みモデルは、精選された COBOL コーパス上での継続事前学習と指示チューニング、そして専用ベンチマーク（MainframeBench）の公開によって、*知識*のギャップに対処している。

### 2.3 リバースエンジニアリングとドキュメント生成

プログラム理解の研究は、レガシーシステム向けのコールグラフ抽出、データフロー解析、クローン/デッドコード検出のツール群を生み出してきた。典型的に欠けてきたのは*ラストマイル*である。すなわち、これらのグラフを、COBOL を知らない Java 開発者が直接行動に移せるようなビジネス可読の設計ドキュメント（SAD/SDD）、デシジョンテーブル、擬似コードへと変換し、それらのドキュメントをトレーサビリティマトリクスを通じてソースおよびテストへ結び付けることである。ModernizeX はドキュメントを、翻訳を実施するのと同じグラウンディングされたパイプラインが生成する、第一級かつバージョン管理された人手レビュー可能な成果物として扱い、*あらゆる*モダンコンポーネントが特定のレガシーコンポーネントと設計セクションへ遡れるという規律を強制する。

---

## 3. ModernizeX の方法論

レガシーソース集合 $S_L$ — COBOL プログラム、コピーブック、JCL、埋め込み SQL、および CICS BMS 画面マップ — が与えられると、ModernizeX はモダンシステム $S_M$（Java 17/21 の Spring Boot / Spring Batch サービス、Vue 3 フロントエンド、および典型的には PostgreSQL であるリレーショナルなターゲットスキーマ）を、それを信頼するために必要な証跡とともに生成する。すなわち、設計ドキュメント $D$（SAD、SDD、テスト計画）、双方向トレーサビリティマトリクス $T$、そして振る舞い等価性の検証結果 $V$ である。

**成功基準。** モダナイゼーションが*完了*したといえるのは、次の三つの条件が同時に成立する場合に限られる。これらは、以下の方法論の各段階が満たすように設計された「完了の定義（definition of done）」を形成する。

1. **トレーサビリティ（traceability）** — あらゆるレガシーコンポーネント（プログラム、コピーブック、JCL ステップ、CICS トランザクション、画面）が、双方向マトリクス $T$ において少なくとも一つのモダンコンポーネントへ対応し、孤立したレガシープログラムも、テストを欠いた要件も存在しない。各対応は設計ドキュメント $D$ を通じて辿ることができるため、レビュアーは任意のレガシー成果物から、その対応するモダンな相手、その設計根拠、そしてそのテストへと辿り着き、いかなるビジネスロジックも黙って失われていないことを承認できる。
2. **正しさ（correctness）** — モダナイズされたシステムは、レガシーシステムのロジックを最大限に保持する。すなわち、$S_L$ に符号化されたあらゆるビジネスルール、計算、条件分岐、エッジケースの挙動が $S_M$ へ引き継がれる。イディオマティックな形へ再構成されはするが、決して削除されたり、近似されたり、捏造されたりすることはない。移行はコードの*形状*（段落からメソッドへ、コピーブックから DTO へ、CICS トランザクションからエンドポイントへ）を変えることはあっても、その*意味論*を変えることは決してない。これにより、移行されたシステムが原システムに対して正しく、かつ完全であることが保証される。
3. **比較可能性（comparability）** — レガシーシステムとモダナイズされたシステムは、同一の入力に対して実行し比較することができる。自動化されたテストに支えられた振る舞い等価性テスト $V$ が、モダンコンポーネントがレガシーの出力を再現することをフィールドごとに実証するため、受け入れはコードの目視検査ではなく、$S_L$ と $S_M$ の間の実測された等価性に立脚する。

ターゲット市場である銀行・保険・行政にとっては、第四の厳格な制約として**データ機密性**がある。すなわち、$S_L$ はいかなる時点においても信頼されないネットワークを通過してはならない。これは Web LLM へのコピー＆ペースト型のワークフローを排除し、展開アーキテクチャを規定する（第 3.10 節）。

本節の残りでは、これらの基準を協働して満たすアーキテクチャと各パイプライン段階について述べる。

### 3.1 アーキテクチャ概要

ModernizeX は**シンクライアント／ファットサーバー**設計に従う。軽量な VS Code 拡張機能（およびチーム向けワークフローのためのマルチプロジェクト WebApp）が、UI、ローカルでのコード行数カウント、ファイル選択、差分レビューを担う。独自のロジックはすべて — COBOL パーサー、静的解析器、IR ビルダー、LLM ゲートウェイ、コードジェネレーター、ビルダー、テストランナー — バックエンドの**ワーカープール**に存在し、長時間ジョブ、リトライ、段階ごとのチェックポイントをサポートする耐久性のあるワークフローエンジン（たとえば Temporal）によってオーケストレーションされる。バックエンドは SaaS テナントとして展開することも、機密性を重視する顧客向けには、**エアギャップ（air-gapped）環境のオンプレミス Docker 展開**として展開することもできる（第 3.10 節）。

本プラットフォームは、移行を**ツール駆動の 7 段階**として実行する。各段階は自動化されており、オペレーターの工数は起動、監視、および各段階の出力レビューに限定される。

| # | 段階 | ツールが行うこと |
| :-- | :---- | :---- |
| 1 | **アセスメント** | ソースツリーを取り込み、プログラム/コピーブック/画面/JCL をインベントリ化する。各プログラムを分類し（バッチ／画面／オンライン）、LOC をカウントし、`CALL` 依存関係をマッピングし、非対応構文にフラグを立てる。 |
| 2 | **リバースエンジニアリング** | 各 COBOL プログラムを完全な XML AST へ解析する。コピーブックを（`REPLACING` を含めて）解決し、シンボルテーブル／IR とコードグラフを構築し、アプリケーションマニフェストとリバース設計ドキュメント（SAD/SDD）を生成する。 |
| 3 | **コード生成** | AST から完全なマルチモジュール Maven プロジェクト（Spring Boot / Spring Batch）を生成する。すなわち、プログラムごとに 1 モジュール、共有ランタイム（`common`）、テキスト／SQL 二重モードのファイル I/O 層、画面マニフェスト、画面ごとの Vue 3 SFC スキャフォールディングである。出力は無修正でコンパイルが通る。 |
| 4 | **コードリファクタリング**（モダナイゼーション） | 生成されたサービスに対する AI 駆動の構造リファクタリングを、振る舞いフィンガープリントの保持とともに実施する。プログラムを並列に実行し、プログラムごとのリファクタリングレポートと差分を生成する。 |
| 5 | **UI/UX モダナイゼーション** | 24×80 の端末エミュレーションを、画面ごとのモダンな Vue コンポーネントで置き換える。ラベルは COBOL 画面のキャプションから収集し、フィールド／検証の挙動を忠実に再現し、任意でデザインシステムによるテーマ適用を行う。 |
| 6 | **データ移行** | COBOL のレコードレイアウトからリレーショナル DDL を導出し（ISAM/VSAM → PostgreSQL）、乖離した顧客スキーマ向けのカラムマッピングを生成し、提供されたサンプル／マスターデータをロードする。 |
| 7 | **検証** | モジュールごとに JUnit テストを生成・実行し（並列、JaCoCo によるカバレッジ集約を伴う）、稼働中のシステムに対して結合テスト仕様を作成・実行する。 |

段階 1〜3 および 6 は**完全に決定論的**（パーサー＋ルールベースジェネレーター）であり、再現可能で、オペレーターの限界工数はほぼゼロである。段階 4、5、7 は、**プログラムごとに並列実行される AI エージェント**と、リファクタリングレポート・テスト結果・承認に対する人手レビューを組み合わせる。設計上、ここに工数予算が存在する。なぜなら、ここが等価性を*証明する*場所だからである。プログラムごとの並列実行は、コードベースが大きくなってもパイプラインを平坦に保つよう設計されている（規模拡大時にも成り立つと我々が期待する性質。第 7 節を参照）。

パイプラインは、各状態が再開可能なチェックポイントとなる明示的なステートマシンを通じてプロジェクトを前進させる。

```
DRAFT → CONFIGURED → ASSESSED → REVERSED → GENERATED
      → REFACTORED → UI_MODERNIZED → DATA_MIGRATED → VALIDATED → COMPLETED
                                  ↘ NEEDS_REVIEW (loop back)
```

永続化は、リレーショナルストア（プロジェクト／実行のメタデータ）、オブジェクトストア（ソーススナップショットと成果物）、そしてコードグラフのためのグラフデータベース（Neo4j/JanusGraph）に分割される。以下の小節では、各パイプライン段階を方法論上のステップとして述べる。

### 3.2 段階 1 — アセスメント

アセスメントはソースツリーを取り込み、いかなるコードも生成される前に、レガシー資産の完全なインベントリとリスク像を生成する。すなわち、種別（バッチ／画面／オンライン）ごとのプログラム数、成果物クラス（プログラム、コピーブック、BMS マップ、JCL）ごとの LOC、`CALL` 依存関係マップ、そして非対応または高リスクな構文へのフラグである。同じパスから、ModernizeX は平均サイクロマティック複雑度、プログラムごとの複雑度スコア（LOC、呼び出し深度、CICS コマンド数、ファイル I/O、コピーブックのファンインを組み合わせた Low/Medium/High/Critical）、およびリスク指標（クローン検出によるデッド／重複コード、`GO TO`/`ALTER`／自己書き換えパターン、DB2 固有の SQL、JCL ユーティリティ依存）を算出する。これにより工数見積りと*自動移行カバレッジ見積り*（自動化可能な割合対手作業を要する割合）が得られる。本段階は完全に自動化されており、約30K-LOC の評価コードベース上での実測されたオペレーターとパイプライン時間は **0.2時間** であった。

### 3.3 段階 2 — グラウンディングされたリバースエンジニアリング

ModernizeX の根本的な設計判断は、**LLM が生のレガシーコードを決して見ない**という点にある。したがって段階 2 は二つのフェーズで進行する。すなわち、構造の決定論的な復元、続いてその構造のドキュメントへのグラウンディングされた翻訳である。

**解析（パーシング）。** 各プログラムは、COBOL 文法（ProLeap/Koopa クラスのパーサー、または内製の ANTLR 文法）を用いて完全な XML AST — 各 DIVISION、データ項目、SCREEN SECTION、文 — へと解析される。その際、方言認識（IBM Enterprise COBOL、Micro Focus、GnuCOBOL）を伴い、コピーブックのフィールドが文脈上で解決されるよう `COPY` ディレクティブ（`REPLACING` を含む）を展開する。埋め込みの `EXEC SQL` および `EXEC CICS` ブロックは抽出され型付けされる。シンボルテーブルと IR は解決済みの AST から構築され、本段階はあらゆるプログラム、画面、データストアを記述した**アプリケーションマニフェスト**を生成する。

**グラフ構築。** AST から、エンジンはシステムの構造を協働して捉えるグラフ群を構築する。

| グラフ | ノード → エッジ | 目的 |
|---|---|---|
| **コールグラフ** | プログラム → プログラム（`CALL`/`LINK`/`XCTL`） | 実行順序、波及範囲、リーフ優先の書き換え順序 |
| **データフローグラフ** | レコード/フィールド → プログラム（読み/書き） | どのプログラムがどのデータを所有するか |
| **コピーブック依存関係** | プログラム ↔ コピーブック | 共有データモデル、変更影響 |
| **ジョブフローグラフ（JCL）** | ステップ → プログラム → データセット | バッチのトリガーと順序付け |
| **DB アクセスグラフ** | プログラム → テーブル/ビュー（CRUD） | スキーマ設計のためのデータ所有権 |
| **画面フローグラフ（CICS）** | BMS マップ → トランザクション → プログラム | API エンドポイントとして再表現される画面 |
| **モジュールクラスター** | 上記に対するコミュニティ検出 | マイクロサービス／境界づけられたコンテキストの候補境界 |

**標準化 IR。** AST とグラフは、単一の正規かつ機械可読な表現（XML/JSON の IR）へシリアライズされる。あらゆる構文要素は明示的に型付けされ、あらゆる相互参照（呼び出されるプログラム、共有されるコピーブック、書き込まれるデータセット）は推測されたものではなく解決済みのリンクである。この IR が、下流のすべての AI 段階が消費する*グラウンディング（grounding）*である。決定論的に生成されるため、その出力は再現可能である。すなわち、変更のないソースに対して解析を再実行すれば同一のグラフが得られ、編集は増分的な再解析のみをトリガーする。

**グラウンディングされたドキュメント化。** IR が与えられると、LLM の役割は構造の復元ではなく、*構造の言語への翻訳*となる。プロンプトは、IR の断片 — あるプログラムの解決済み入出力、その段落、そのコピーブックレイアウト、その CICS トランザクション — によって埋められるテンプレート化されたスロットから組み立てられる。したがってモデルは、テキストウィンドウからグローバルなアーキテクチャを推論するのではなく、*既知の事実を説明し再表現する*ことを求められる。このグラウンディングこそが、信頼できないテキスト予測器を、境界づけられ監査可能なコンポーネントへと変えるものである。本フェーズは四つの成果物を生成する。

- **SAD（Software Architecture Document）:** コンテキスト図とコンテナ図、デプロイメントビュー、外部インターフェース（メッセージキュー、IMS、DB2）、および横断的関心事。これらはコールグラフと DB アクセスグラフから導出される。
- **SDD（Software Design Document）:** 各プログラムについて、その入出力、擬似コードとデシジョンテーブルで表現されたビジネスルール、コピーブックからのデータ構造、`EXEC SQL`/DCLGEN からの DB スキーマ。抽象化戦略に従い、ビジネスロジックはターゲット中立の擬似コードでレンダリングされるため、Java または C# の開発者は COBOL を読むことなく実装できる。
- **テスト計画:** UT/IT/回帰の範囲、データ戦略、環境要件。
- **トレーサビリティマトリクス:** 背骨となる成果物（第 3.9 節）。

あらゆるドキュメントとセクションはバージョン管理され、レビュー状態（`DRAFT / IN_REVIEW / APPROVED`）を保持し、インライン編集、コメント、バージョン間の差分をサポートする。これが出力を信頼できるものにする人間参加型の規律である。実測された段階時間: 約 30K-LOC の評価コードベース上で **2時間**。

### 3.4 段階 3 — ルールベースのコード生成

コード生成は意図的に**決定論的**である。ルールベースのジェネレーター（プラガブルで顧客固有のルールパック — COBOL パターン → ターゲットパターン、命名ルール、例外処理ルール）が、IR を断片ではなく完全でビルド可能なターゲットプロジェクトへとトランスパイルする。

- **マルチモジュール Maven プロジェクト**（Spring Boot / Spring Batch）。**COBOL プログラムごとに 1 モジュール**に加え、共有ランタイムモジュール（`common`）を備える。
- **テキスト／SQL 二重モードのファイル I/O 層**。これにより生成されたプログラムは、最初はフラットファイルに対して、データ移行（段階 6）後はリレーショナルストアに対して実行できる。
- 各画面プログラムのための**画面マニフェスト**と**画面ごとの Vue 3 SFC スキャフォールディング**。これらは段階 5 で消費される。
- 生成された各ユニットへの来歴アノテーション（`@cobol-origin: PROGRAM-X line 123-145`）と、サイドバイサイドビューアで表示される多層の**旧↔新マッピング**（プログラム↔サービス、段落↔メソッド、コピーブック↔DTO、JCL ステップ↔ジョブ、CICS トランザクション↔エンドポイント、VSAM↔テーブル）。

出力は**無修正でコンパイルが通る** — これは実測されたすべての実行で検証された性質であり、本段階が約 30K LOC において **0.3時間** で済む理由である。移行は**増分的**である。すなわち、顧客はモジュール単位で移行でき、マッピングは 1 対 N の分割と N 対 1 の統合をサポートする。

### 3.5 段階 4 — AI コードリファクタリング（モダナイゼーション）

ルールで生成されたコードは正しいが非イディオマティックである。段階 4 は、生成されたサービスに対して **AI 駆動の構造リファクタリング**を適用する。すなわち、ほぼ同一の段落の重複排除、長いメソッドの凝集した単位への分解、コピーブックの DTO／エンティティへのマッピング、CICS トランザクションの REST エンドポイントとしての再表現と JCL ステップのスケジュールジョブとしての再表現、そして意図を明らかにする名前への改名である。二つの性質が本段階を安全かつスケーラブルにする。

- **振る舞いフィンガープリント（behavioral fingerprint）の保持。** 各リファクタリングは、リファクタリング前ユニットの振る舞いフィンガープリントに照らして検査されるため、再構成が意味論を黙って変えることはない。すなわち、LLM は*形状*を改善するのであって、*意味*を改善するのではない。
- **プログラムごとの並列実行** — 自動ジョブサイジングを伴い、人手レビューのためにプログラムごとのリファクタリングレポートと差分を生成する。

**ビルド＆検証ループ。** リファクタリングされたコードは自動的にコンパイルされ（`mvn compile`）、任意で静的解析（SpotBugs/SonarQube）が行われる。失敗と低信頼度の箇所は、**レビューレポート（Review Report）**へ送られ、人間が解決しなければならないものを正確に列挙する。すなわち、マッピング不可能なパターン（`ALTER`、複雑な `REDEFINES`、特殊な CICS）、残存するビルド／lint エラー、欠落した依存関係であり、それぞれにワンクリックで割り当て可能な LLM パッチの提案が付き、修正イテレーションを通じて差分が追跡される。実測された段階時間: 約 30K-LOC の評価コードベース上で **10時間** — 最大の AI 段階コストであり、リファクタリングレポートの人手レビューが支配的である。

### 3.6 段階 5 — UI/UX モダナイゼーション

段階 5 は、24×80 の端末エミュレーションを**画面ごとのモダンな Vue コンポーネント**で置き換える。ラベルは COBOL 画面自身のリテラルキャプションから収集され、フィールドと検証の挙動は SCREEN SECTION / BMS 定義から忠実に再現され、任意でデザインシステムのテーマを適用できる。画面マニフェストと SFC スキャフォールディングは段階 3 からすでに存在するため、本段階はパイプラインの観点では安価である（約 30K LOC において **約0.5時間**）。より深い UI/UX の*再設計* — 顧客のユーザーとの確認サイクルを通じた反復 — は下流のデリバリー作業であり、意図的に自動化パイプラインの外に置かれている。

### 3.7 段階 6 — データ移行

段階 6 は、COBOL のレコードレイアウトから直接リレーショナル DDL を導出し（ISAM/VSAM → PostgreSQL テーブル）、顧客のターゲットスキーマが導出されたものと乖離する場合にはカラムマッピングを生成し、提供されたサンプル／マスターデータをロードする。段階 3 の二重モードファイル I/O 層と組み合わせることで、移行されたシステムはビジネスロジックに手を触れることなく、ファイルベースから SQL ベースの永続化へ切り替えられる。実測された段階時間: 約 30K LOC において **約1時間**。

### 3.8 段階 7 — 検証（振る舞い等価性）

信頼は最終的に、$S_M$ が $S_L$ のように振る舞うことを示すことに立脚する。検証には二つの実測されたサブ段階がある。

- **単体テスト。** JUnit テストは、リバースエンジニアリングされた振る舞いと、利用可能な任意のサンプル入出力から、モジュールごとに生成され、**モジュール横断で並列に**実行される。JaCoCo のカバレッジは、設定可能なカバレッジ下限に対して単一のレポートへ集約される。
- **結合テスト。** パイプラインはジョブフローグラフと画面フローグラフから結合テスト仕様を生成し、稼働中のシステムに対してそれを実行する。

等価性の比較のため、ModernizeX はテストケースを**自動実行可能**（入出力データセットが明確に定義されたバッチプログラム、UI 非依存のサービス、SQL ストアドプロシージャ）または**手動のみ**（3270 画面やスタブ化できない外部システムに依存するケース）に分類し、ユーザーによる上書きを許す。自動実行可能なケースについては、ハーネスがレガシー環境（メインフレームまたはエミュレーター）とモダンサービスを同一の入力で実行し、設定可能なルールの下で出力を比較する。

- **ビット完全一致**（固定長／バイナリレコード）、
- **フィールドレベル**（コピーブックレイアウトに照らしてパースし、フィールドごとに比較）、
- **数値許容誤差**（浮動小数点に対する ±ε）、および
- **順序非依存**（順序付けされていないデータセット）。

フィールドごとの差分ビューアが不一致を強調表示し、レポートは*「新しいコードが誤っているための失敗」*と*「テストデータが異なるための失敗」*を区別する。この区別は生産的なデバッグに不可欠である。結果は実行をまたいでバージョン管理され、トレーサビリティマトリクスへ逆リンクされる。検証は意図的に最も高価な段階であり — 約 30K LOC において **24時間** — その時間はコードを*書く*ことにではなく、等価性を*証明する*ことに費やされるからである。

### 3.9 トレーサビリティと人間参加型

トレーサビリティマトリクスは、いかなるビジネスロジックも黙って失われないことを保証する成果物であり、これはユーザー受け入れテスト中に移行プロジェクトを最も頻繁に沈没させる失敗モードである。ModernizeX はこれを静的なファイルではなく、検索可能でフィルター可能な構造として維持し、SAD 要件 ↔ SDD セクション ↔ ソースプログラム ↔ 単体テスト ↔ 結合テストを双方向にリンクする。セルはクリックすると基礎となる成果物へ移動でき、システムはテストのない要件や SDD エントリのないソースについて警告する。これは完了の定義を運用可能にするものである。すなわち、レガシープログラムが完了したといえるのは、等価なモダンロジックが存在し、自動テストがそれを証明し、ビジネスアナリストが出力の一致を承認したときに限られる。

### 3.10 セキュリティ、展開、およびライセンシング

規制対象の購買層に対する中核的な価値提案が*「あなたのソースコードはネットワークの外へ一切出ない」*ことであるため、セキュリティアーキテクチャは後付けではなく、方法論の第一級の一部である。

- **BYOK ／モデル非依存ルーティング。** 顧客は自身の LLM 鍵またはエンドポイントを提供する。ローカルまたはセルフホストのモデルが使われる場合、ソース由来の IR は顧客のインフラストラクチャから一切出ない — 「LLM 外部流出なし（no-LLM-egress）」モードである。
- **エアギャップ Docker 展開。** 最も厳格な顧客向けには、バックエンドは顧客インフラ上で完全に稼働する Docker イメージとして出荷され、VS Code 拡張機能は内部 URL を指す。
- **知的財産（IP）保護。** 独自のエンジンはイメージ内で難読化されたバイナリへコンパイルされるため、コンテナへアクセスできてもアルゴリズムは復元できない。
- **暗号学的ライセンシングと従量課金。** 権利（LOC 上限、有効期限）は、バイナリに埋め込まれた公開鍵によって検証される署名済みライセンスファイルとして発行される。オフラインの使用量は改竄検知可能な暗号化ローカルストア（ロールバック検知のためにハッシュチェーン化）で計測され、外部への通信を行うことなく実行可能 LOC あたりの課金を可能にする。

非機能要件がプラットフォームを完成させる。すなわち、OAuth2/OIDC の SSO、RBAC、改変不能な監査ログ、保存時の AES-256 と転送時の TLS 1.3、構造化されたログ／メトリクス／トレーシング、そして 30 分以内での約 500K LOC の解析スループットである。

---

## 4. 評価の設定

我々は ModernizeX を、予測ではなく**実在する完全な移行に対する実測工数**で評価する。すなわち、段階ごとの時間は実際のパイプライン実行で記録され（各段階を起動・監視・レビューするオペレーターの時間と、パイプライン実行時間を含む）、すべてのソースコード統計はリポジトリから直接計測された。この評価は、検証までエンドツーエンドで移行された一つの完全なコードベースからなる。

### 4.1 対象: SAKURA-SMS — 販売管理（対話画面＋バッチ）

*SAKURA-SMS* は、対話型端末プログラムに加え、索引/順編成ファイル上でのバッチ処理として構築された完全な販売管理システムである。

| 指標 | 値 |
|---|---|
| COBOL プログラム | **68**（.cob） — 60 メイン + 1 メニュー + 7 サブ |
| COBOL LOC | **30,635** |
| コピーブック | **72**（1,106 LOC） |
| 画面 | **SCREEN SECTION を持つ 46 プログラム**（端末 UI） |
| バッチ／呼び出し可能サブ | 22 プログラム |
| データベース | 索引/順編成ファイル（ISAM）。CICS なし、埋め込み SQL なし |

SAKURA-SMS は SCREEN SECTION の対話型 UI 経路と ISAM（索引/順編成）ファイル意味論を行使する。CICS も埋め込み SQL も持たず、これはミッドレンジ COBOL 資産に共通する対話＋バッチのプロファイルである。

### 4.2 計測方法

- **予測ではなく実測。** 段階ごとの時間は、アプリケーションチームによって記録された実際のパイプライン実行から得られる。これらは各段階を起動・監視・レビューするオペレーターの時間を含む。ソース統計（プログラム数、LOC、コピーブック、画面）はリポジトリ上で直接計測された。
- **検証済みの出力。** 第 5.2 節で報告されるビルド状態、モジュールインベントリ、テストキャンペーン結果は、ツールログのみからではなく、生成されたリポジトリから検証されている。
- **ベースライン。** 同一のコードベースを並行して手作業で移行したチームは存在しない。SAKURA-SMS の完全手作業の数値は、計測ではなく、大まかで所有者が調整可能な見積り（> 12人月）である。
- **人月換算:** 1 MM ≈ 21営業日 × 8時間 = 168時間。

---

## 5. 結果

### 5.1 段階ごとの実測工数

以下の時間は、Cobol2Java ジェネレーターを用いた ModernizeX による、段階ごとの実際の実測工数（operator ＋パイプライン時間）である。

| 段階 | SAKURA-SMS |
| :---- | ---: |
| 1. アセスメント | 0.2時間 |
| 2. リバースエンジニアリング | 2時間 |
| 3. コード生成 | 0.3時間 |
| 4. コードリファクタリング | 10時間 |
| 5. UI/UX モダナイゼーション | 0.5時間 |
| 6. データ移行 | 1時間 |
| 7a. 検証 — 単体テスト | 12時間 |
| 7b. 検証 — IT 仕様 | 12時間 |
| **合計** | **38時間** |

これらの数値の読み方を構造づける観察が二つある。

- **決定論的な段階はほぼ無償である。** アセスメント、リバースエンジニアリング、生成、データ移行を合わせても **≤ 3.5時間** で済む。これらは完全に自動化されている（パーサー＋ジェネレーター）ためであり、段階 3 の出力は手作業のパッチ当てなしでコンパイルが通る。
- **工数は検証が存在する場所に集中する。** リファクタリング（10時間）と検証（24時間）が支配的である。これらの段階は AI エージェントを並列に実行するが、リファクタリングレポート・テスト結果・IT 仕様承認に対する人手レビューを保持する。これは設計によるものである。すなわち、その時間はコードを*書く*ことにではなく、等価性を*証明する*ことに費やされる。プログラムごとの並列実行は、コードベースが大きくなってもこのプロファイルを平坦に保つよう設計されている。より大規模な資産でその規模拡大を定量化することは今後の課題である（第 7 節）。

### 5.2 検証済みの段階出力

**SAKURA-SMS。**

- *生成:* 68 プログラム → Maven リアクター `batch-app-web`（**66プログラムモジュール** ＋共有 `common`）および `batch-app-batch`（**2バッチモジュール**）。フルビルドは**クリーンにコンパイルが通る**。
- *UI:* 画面プログラムはプログラムごとの Vue 3 SFC フォルダ（36プログラムUIフォルダー）として再生成され、ラベルは COBOL 画面自身のリテラルキャプションから取得された。
- *検証:* 単体テストキャンペーンは **68/68モジュール** について完了し、モジュールごとのレポートと集約された JaCoCo カバレッジレポートを伴う。

### 5.3 手作業ベースラインとの比較

SAKURA-SMS を並行して手作業で再移行したチームは存在しないため、以下の比較は実測されたパイプライン工数と*見積もられた*手作業ベースラインとの間のものであり、統制された結果ではなく参考値として読むべきである。

| コードベース | 規模 | 完全手作業（見積り、所有者が調整可能） | ModernizeX パイプライン（実測） |
| :---- | :---- | :---- | :---- |
| SAKURA-SMS | 30,635 LOC | **> 12人月**（≈ 4 人 × 3 か月） | **38時間** |

この 38時間は、**検証までの**パイプライン段階（生成された UT の実行、IT 仕様の実行）を対象とする。下流の受け入れ、UAT、変更要求の工数は各プロジェクトの範囲に依存し、この計測には含まれていない。

**コストへの含意。** 単価（計算資源、エンジニアリング）は展開ごとに固有であり、本稿では意図的に除外している。外部の市場アンカーとして、変換のみのツールは 1 行あたり US$2.75 と公開価格が付けられている [6]。これはテスト・レビュー・結合の*前*の、コード変換のみをカバーする数値である。それに対し、実測された ModernizeX パイプライン全体は、これに加えて実行済みの単体テストと結合テスト仕様を含む。

---

## 6. 考察、限界、および妥当性への脅威

**メカニズムはグラウンディングであってモデルではない。** 実測された工数プロファイルは中核的な主張を支持する。いかなるモデルも構造を推測するよう求められない段階 — アセスメント、リバースエンジニアリング、生成、データ移行 — は、約 30K-LOC のコードベース上で ≤ 3.5 時間で済み、手作業のパッチ当てなしでコンパイルが通る出力を生成する。これはまさに、それらが IR の決定論的な変換だからである。AI 支援の段階は、その時間をコードの記述にではなく、*等価性の証跡の人手レビュー*（リファクタリングレポート、テスト結果、IT仕様承認）に費やす。この分業により、ModernizeX はモデル選択に対して頑健となり、XMainframe [1] のようなドメイン適応済みバックエンドから直接恩恵を受けられる。

**妥当性への脅威。** 手作業工数との比較は、並行計測ではなく*見積り*に立脚している。SAKURA-SMS を手作業で移行したチームは存在せず、> 12人月という数値は大まかで所有者が調整可能な見積りである。実測された評価には統制されたバニラ LLM のアーム（対照群）が含まれないため、素朴な LLM 翻訳に対する優位性はここで計測されるのではなく、アーキテクチャ的に論じられる。評価は単一の約 30K-LOC コードベースに立脚しており、これは一般化を制約する。すなわち、はるかに大規模な資産や他の方言における挙動は、これらの計測では確立されていない。オペレーターの時間はチームのツールへの習熟度に依存し、38時間の合計は下流の受け入れ、UAT、変更要求の工数を除外している。これらは各プロジェクトの範囲によって変動する。絶対的な工数は、方言と、マッピングが困難な構文（`ALTER`、自己書き換えコード、複雑な `REDEFINES`、特殊な CICS）の密度によっても変動する。

**本質的な限界。** 完全な自動化は目標でも成果でもない。すなわち、マッピング不可能なパターンと低信頼度の箇所は人手による解決のためレビューレポートへ送られ、リファクタリングと検証は意図的に人手レビューと承認を保持し、振る舞い等価性テストは手作業なしには画面依存またはハードウェア依存のケースをカバーできない。深い UI/UX の*再設計* — エンドユーザーとの反復 — は、パイプラインの外の下流デリバリー作業のままである。テスト比較（Test Compare）は、レガシー実行環境または忠実なエミュレーターへのアクセスと、代表的なテストデータに依存する。最後に、オンプレミスのエアギャップ展開はライセンシングとアップデート配信を複雑にする。これは暗号学的な計測方式によって緩和されるが、解消はされない。

**スコープ。** 現行バージョンは COBOL→Java（Spring Boot / Spring Batch ＋ Vue 3）を対象とする。他のレガシー言語（RPG、C/C++、PL/I、Natural、Easytrieve）および追加のターゲットプラットフォーム（.NET）はロードマップ項目である（第 7 節）。

---

## 7. 結論と今後の課題

我々は ModernizeX を提示した。これは COBOL モダナイゼーションを*グラウンディングされた*問題として再構成するエンドツーエンドのプラットフォームである。すなわち、決定論的な静的解析が、言語モデルには確実に推論できない構造を復元し、その後モデルは、ルールで生成されコンパイルが通る基盤の上でのリファクタリング、画面モダナイゼーション、検証に限定される。これらはすべて、独自のソースを顧客の境界内に留めるエアギャップ展開の内部で行われる。実測された証跡は具体的である。すなわち、完全な約 30K-LOC のコードベース — SAKURA-SMS（対話画面＋ISAM ファイル上のバッチ処理） — が **38時間** でエンドツーエンド移行され、コンパイルが通るマルチモジュールの Spring Boot / Spring Batch プロジェクト、モダナイズされた Vue フロントエンド、全 68 モジュールにわたって実行され集約カバレッジを伴う単体テストキャンペーン、そして結合テスト仕様を生成した。工数はあるべき場所に集中した。すなわち、コードを書くことではなく、振る舞い等価性を*証明する*ことにであり、規制されたモダナイゼーションが要求するトレーサビリティ、正しさ、比較可能性の基準を満たした。

今後の課題は、研究の方向性とプロダクトロードマップの双方にまたがる。

**研究の方向性:** (1) 手作業およびバニラ LLM 翻訳に対する、同一コードベースでの統制された比較。これにより第 5.3 節の見積りベースラインを、形式的な精度指標を伴う並行計測へ変換する。(2) 数百 KLOC 規模のより大規模な資産にわたるスケーリング研究。これによりパイプライン工数が規模とともにどう増大するかを特徴づける。(3) ドメイン適応済みのメインフレーム LLM をリファクタリングおよび検証の各段階へ組み込み、同一のグラウンディングの下で汎用モデルに対する限界利得を計測する。(4) 境界づけられたコンテキストのマイクロサービス境界を自動的に提案する、より豊かなモジュールクラスタリング。そして (5) 展開モデルごとに単価が確定した段階で、eLOC あたりのコスト数値を公開する。

**プロダクトロードマップ。** プラットフォームのビジョンは、ModernizeX を三つの軸に沿って拡張する。

1. **従来型 SDLC ツールチェーンとの相互運用性。** 生成されるすべての成果物 — SAD/SDD、トレーサビリティマトリクス、テスト計画、テストレポート — は、顧客がすでに扱っている標準フォーマットでエクスポート可能となる。すなわち、レビューと承認のワークフローのためのオフィス文書（DOCX、Excel など）、そして **Codebeamer** や **Enterprise Architect** のような ALM およびモデリングツールへインポート可能な構造化交換フォーマット（たとえば要件／トレーサビリティのための ReqIF、設計モデルのための XMI）である。これにより、パイプラインの出力はプラットフォーム内だけに留まるのではなく、顧客の既存の要件管理・レビュー・監査プロセスへ直接組み込める。
2. **追加のレガシーソース言語。** グラウンディングされた IR パイプラインを COBOL を超えて他のレガシー資産 — **RPG**（IBM i）および **C/C++** — へ拡張し、同じアーキテクチャを再利用する。すなわち、言語フロントエンドごとの決定論的な解析とグラフ構築、共有 IR、そして同じ生成／リファクタリング／検証の各段階である。
3. **追加のモダナイゼーションターゲット。** 同じ IR と顧客ルールパックから、Java と並行して **.NET**（C#/ASP.NET Core）を生成する。これにより、ターゲットプラットフォームは別個のプロダクトではなく設定上の選択となる。

---

## References

[1] XMainframe: A Large Language Model for Mainframe Modernization. arXiv:2408.04660. https://arxiv.org/html/2408.04660v1

[2] ModernizeX 内部ドキュメント — *WebApp Requirement Specification (COBOL Migration Platform), v1.0*、*Design & Traceability Strategy*、*ModernizeX Effort Evaluation v1.0*（SAKURA-SMS 移行に関する、段階ごとの実測工数、ソースプロファイル、および検証済み出力）。

[3] ProLeap / Koopa COBOL parsers; ANTLR. 解析エンジンが参照するオープンソースの COBOL 解析ツール群。

[4] Temporal — 長時間実行されチェックポイント化されるパイプラインのための、耐久性のあるワークフローオーケストレーション。

[5] A. Irrera, "Banks scramble to fix old systems as IT 'cowboys' ride into sunset." *Reuters*, April 10, 2017. https://www.reuters.com/article/us-usa-banks-cobol-idUSKBN17C0D8 — オーストラリア・コモンウェルス銀行の基幹 COBOL プラットフォーム刷新（2012 年開始）は 5 年を要し、≈US$7億4,990万を要した。米国の銀行システムの推定 43% と 2,200 億行の COBOL が依然として使用されている。

[6] Amazon Web Services, "AWS Mainframe Modernization — Pricing." https://aws.amazon.com/mainframe-modernization/pricing/ (2026年7月にアクセスした) — パートナーが実行するコード変換は 1 行あたり US$2.75 と表示されている。この価格は変換サービスのみをカバーし、テスト・レビュー・結合・ランタイムは別途価格設定される。ここでは外部の市場価格アンカーとしてのみ引用する。

[7] U.S. Government Accountability Office, "Information Technology: Federal Agencies Need to Address Aging Legacy Systems." GAO-16-468, May 2016. https://www.gao.gov/assets/gao-16-468.pdf — レガシーシステムに対する数十億ドル規模の運用・保守支出と、COBOL スキルを持つ人材の希少性（および割増コスト）を文書化している。

---

*ワーキングペーパー — 図（アーキテクチャ図、パイプラインのステートマシン、依存関係グラフの例）は、カメラレディ版において `docs/design_docs/` の SVG アセットから挿入される予定である。生成された SAKURA-SMS ターゲットプロジェクトへのリポジトリリンクは、公開前に追加される予定である。*
