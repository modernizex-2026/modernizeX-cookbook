# ModernizeX — COBOL → Java 移行工数評価

**対象:** SAKURA-SMS（販売管理）。

本評価は、**ModernizeX** パイプラインと自社の **Cobol2Java ジェネレーター** を組み合わせて、完全な COBOL コードベースをモダンな Java/Vue スタックへ変換するために実際に要した工数を測定したものである。予測ではなく、以下に示す各ステージの工数はパイプラインの**実運用における実測値**であり、ソースコードの統計値はリポジトリから直接測定した。

---

## 1. パイプライン — 7 ステージ

ModernizeX は、移行をツール駆動の 7 つのステージとして実行する。各ステージは自動化されており、測定した工数には各ステージを起動・監視・レビューするためのオペレーター作業時間を含む。

| # | ステージ | ツールの動作内容 |
| :-- | :---- | :---- |
| 1 | **Assessment（アセスメント）** | ソースツリーを取り込み、プログラム/コピーブック/画面/JCL をインベントリ化する。各プログラムを分類（バッチ / 画面 / オンライン）し、LOC をカウントし、CALL 依存関係をマッピングし、非対応の構文をフラグ付けする。 |
| 2 | **Reverse Engineering（リバースエンジニアリング）** | すべての COBOL プログラムを完全な XML AST（各 DIVISION、データ項目、SCREEN SECTION、ステートメント）へ解析する。コピーブック（REPLACING を含む）を解決し、シンボルテーブル / IR を構築し、アプリケーションマニフェストとリバース設計ドキュメントを出力する。 |
| 3 | **Code Generation（コード生成）** | AST から完全なマルチモジュール Maven プロジェクト（Spring Boot / Spring Batch）を生成する。プログラムごとに 1 モジュール、共有ランタイム（`common`）、テキスト/SQL デュアルモードのファイル I/O 層、画面マニフェスト、画面ごとの Vue 3 SFC スキャフォールディングを含む。出力はそのままビルド可能である。 |
| 4 | **Code Refactoring（コードリファクタリング）**（モダナイゼーション） | 生成されたサービスに対する AI 駆動の構造リファクタリング（ほぼ同一の段落の重複排除、長大なメソッドの分解、意図を明確にする名称へのリネーム）を、振る舞いのフィンガープリントを保持したまま実行する。プログラムを**並列**で実行（ジョブサイズを自動調整）し、プログラムごとのリファクタリングレポート + 差分を出力する。 |
| 5 | **UI/UX Modernization（UI/UX モダナイゼーション）** | 24×80 のターミナルエミュレーションを、画面ごとのモダンな Vue コンポーネントに置き換える。ラベルは COBOL の画面キャプションから抽出し、フィールド/バリデーションの振る舞いを忠実に再現する。デザインシステムのテーマ適用はオプションである。 |
| 6 | **Data Migration（データ移行）** | COBOL のレコードレイアウトからリレーショナル DDL を導出（ISAM/VSAM → PostgreSQL テーブル）し、顧客側の異なるスキーマ向けにカラムマッピングを生成し、提供されたサンプル/マスターデータをロードする。 |
| 7 | **Validation（検証）** | モジュールごとに JUnit テストを生成（並列実行、JaCoCo によるカバレッジ集計付き）し、稼働中のシステムに対する結合テスト仕様を作成・実行する。 |

---

## 2. ソースプロファイル（実測）

### 2.1 SAKURA-SMS — 販売管理（対話型画面 + バッチ）

| メトリクス | 値 |
| :---- | :---- |
| COBOL プログラム | **68**（.cob）— メイン 60 + メニュー 1 + サブ 7 |
| COBOL LOC | **30,635** |
| コピーブック | **72**（1,106 LOC） |
| 画面 | **SCREEN SECTION を持つ 46 プログラム**（ターミナル UI） |
| バッチ / 呼び出し可能サブ | 22 プログラム |
| データベース | インデックス/順次ファイル（ISAM）。CICS なし、埋め込み SQL なし |

---

## 3. ステージごとの実測工数

以下の工数は、ModernizeX + Cobol2Java ジェネレーターを用いた**実測工数**（ステージごとのオペレーター作業 + パイプライン実行時間）である。

| ステージ | SAKURA-SMS |
| :---- | ---: |
| 1. Assessment | 0.2 h |
| 2. Reverse Engineering | 2 h |
| 3. Code Generation | 0.3 h |
| 4. Code Refactoring | 10 h |
| 5. UI/UX Modernization | 0.5 h |
| 6. Data Migration | 1 h |
| 7a. Validation — 単体テスト | 12 h |
| 7b. Validation — IT 仕様 | 12 h |
| **合計** | **38 h** |
| リポジトリ | *(link)* |

数値の読み取り方:

- **決定的なステージはほぼコストがかからない。** Assessment、リバースエンジニアリング、生成、およびデータ移行を合わせても、この約 30K-LOC のコードベースに対して **≤ 3.5 h** で完了する。これらは完全に自動化されている（パーサー + ジェネレーター）ためであり、Stage 3 の出力は手作業のパッチ適用なしでビルドできる。
- **工数は検証の存在する箇所に集中する。** リファクタリング（10 h）と検証（24 h）が支配的である。これらのステージは AI エージェントを並列で実行するが、リファクタリングレポート、テスト結果、IT 仕様のサインオフに対する人手のレビューを維持している。これは意図的な設計であり、工数はコードを*書く*ことではなく等価性を*証明する*ことに費やされている。
- **フラットにスケールするよう設計されている。** プログラムごとの並列実行は、コードベースが大きくなってもこの工数プロファイルをフラットに保つことを意図している。より大規模な資産におけるこのスケーリングの定量化は今後の課題である。

---

## 4. 各ステージの成果物（検証済みの結果）

### SAKURA-SMS

- **生成:** 68 プログラム → Maven リアクター `batch-app-web`（**66 プログラムモジュール** + 共有 `common`）および `batch-app-batch`（**2 バッチモジュール**）。フルビルド: **クリーンにコンパイル**。
- **UI:** 画面プログラムをプログラムごとの Vue 3 SFC フォルダとして再生成（36 プログラム UI フォルダ）。ラベルは COBOL 画面自身のリテラルキャプションから取得。
- **検証:** **68/68 モジュール**に対する単体テストキャンペーンを完了し、モジュールごとのレポートと集計された JaCoCo カバレッジレポートを作成。

---

## 5. 手作業ベースラインと比較

SAKURA-SMS を並行して手作業で再移行したチームは存在しないため、以下の比較は実測したパイプライン工数と*推定*した手作業ベースライン（オーナーが調整可能）との比較であり、統制された結果ではなく参考値として読むべきである。

| コードベース | 規模 | 完全手作業（推定） | ModernizeX パイプライン（実測） |
| :---- | :---- | :---- | :---- |
| SAKURA-SMS | 30,635 LOC | **> 12 man-months**（≈ 4 people × 3 months） | **38 h** |

注記:

- 38 h の数値は、**検証までのパイプラインステージ**（生成された UT の実行、IT 仕様の実行）をカバーする。下流の受入/機能拡張の工数は各プロジェクトの UAT および変更要求の範囲に依存し、本測定の対象外である。
- man-month 換算: 1 MM ≈ 21 working days × 8 h = 168 h。

---

## 6. コストサマリー

*(単価は意図的に空欄としている — プロジェクトオーナーが記入すること。)*

### 前提条件

| パラメータ | 値 |
| :---- | :---- |
| パイプライン/計算処理レート | $ ___ / hour |
| エンジニアレート（運用 + レビュー） | $ ___ / hour |
| 為替レート参考値 | 1 USD ≈ ___ VND |

### ソースごとのコスト（ModernizeX パイプライン）

| コードベース | 実測時間 | パイプライン/計算処理コスト | 人的コスト | 合計 |
| :---- | ---: | :---- | :---- | :---- |
| SAKURA-SMS | 38 h | $ ___ | $ ___ | **$ ___** |

### 手作業との比較（同一コードベースに対して）

| コードベース | 完全手作業の工数 | 手作業コスト | ツール支援コスト | 削減率 |
| :---- | :---- | :---- | :---- | :---- |
| SAKURA-SMS | > 12 man-months（推定） | $ ___ | $ ___ | ___ % |

---

## 7. 注記と出所

- **予測ではなく実測:** ステージごとの工数は、アプリケーションチームが記録した実際のパイプライン実行に由来する。ソース統計はリポジトリ上で直接測定した（プログラム数、LOC、コピーブック、画面）。
- **生成結果は検証済み:** SAKURA のビルドステータス（66 + 2 modules がクリーンにコンパイル）および 68/68 の単体テストキャンペーンは、本ワークスペースの生成済みリポジトリから検証済みである。
- **ベースライン:** SAKURA-SMS を並行して手作業で移行したチームは存在しない。「> 12 man-months」の完全手作業の数値は概算であり、オーナーが調整可能な推定値として、オーナーによる調整対象としてマークされている。
